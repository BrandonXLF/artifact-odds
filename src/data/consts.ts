/**
 * Constants common to all supported games. Can be move to
 * {@link GameData} if required in the future.
 * 
 */
import { GameData } from "./data";

export const SUB_STAT_COUNT = 4;
export const LOWER_ROLL_COUNT = 4;
export const UPPER_ROLL_COUNT = 5;
